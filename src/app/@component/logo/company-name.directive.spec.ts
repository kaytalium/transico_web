import { CompanyNameDirective } from './company-name.directive';

describe('CompanyNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CompanyNameDirective();
    expect(directive).toBeTruthy();
  });
});
